<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Minimalist black and white design -->
  
  <!-- Background: Pure black with rounded corners -->
  <rect width="512" height="512" rx="115" fill="#000000"/>
  
  <!-- Main icon: Stylized "N" for NotAIs -->
  <g transform="translate(256, 256)">
    <!-- Bold geometric N shape -->
    <path d="M -80,-120 L -80,120 L -40,120 L -40,-40 L 40,120 L 80,120 L 80,-120 L 40,-120 L 40,40 L -40,-120 Z" 
          fill="#ffffff"/>
    
    <!-- Subtle AI indicator dot -->
    <circle cx="100" cy="-100" r="16" fill="#ffffff"/>
  </g>
</svg>
